# Build artifacts
/target
/dist
**/target          # catch nested target/ that rust-analyzer creates per-package

# Reef-specific cache (generated state, not config)
/.reef/cache/

# Local data
/data
*.db
*.db-journal
*.db-shm
*.db-wal

# Environment / secrets
.env
.env.local
.env.*.local

# OS / editor
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json

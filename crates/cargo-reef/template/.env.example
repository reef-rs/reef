# Copy this file to `.env` and fill in real values.
# `.env` is gitignored.

# Database — embedded libSQL by default (file path).
DATABASE_URL=./data/reef.db

# Logging — comma-separated env_logger / tracing-subscriber filter.
# Examples: "info", "debug", "reef=debug,tower=warn".
RUST_LOG=info
